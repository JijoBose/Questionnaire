<p id="notice"><%= notice %></p>

<p>
  <strong>Survey:</strong>
  <%= @question.title %>
</p>

<p>
  <strong>Options:</strong><br>
  <% list_all_options(@question.id).each do |question| %>
      <%= question.content %><br>
  <% end %>
</p>

<%= link_to 'Edit', edit_question_path(@question) %> |
<%= link_to 'Back', questions_path %>
